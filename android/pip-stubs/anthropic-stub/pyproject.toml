[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "anthropic"
version = "0.86.0"
description = "Android/Chaquopy placeholder for Hermes Agent builds"
requires-python = ">=3.11"
