Hermes Agent v0.13.100

This Android release pins the embedded Linux command-suite package set used by release and F-Droid builds.

- Added a Termux package lock file for reproducible Android Linux asset generation.
- Added mirror fallback for Termux package downloads while preserving SHA-256 verification.
- Kept the Android release version metadata aligned for F-Droid checkupdates.
